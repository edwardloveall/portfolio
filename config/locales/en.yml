en:
  date:
    formats:
      default: "%m/%d/%Y"
      with_weekday: "%a %m/%d/%y"
      # Rails .rfc822 format doesn't include the leading zero on the date for
      # Date objects :(
      rfc822_with_leading_zero_date: "%a, %d %b %Y %T %z"
  flashes:
    project:
      create:
        error: The project could not be created.
      update:
        error: The project could not be updated.
      delete:
        error: The project could not be deleted.
    song:
      create:
        error: The song could not be created.
      update:
        error: The song could not be updated.
      delete:
        error: The song could not be deleted.
  helpers:
    submit:
      authorization:
        create: Authorize
      external_post:
        create: Create Post
        update: Update Post
        delete: Delete Post
      internal_post:
        create: Create Post
        update: Update Post
        delete: Delete Post
      project:
        create: Create Project
        update: Update Project
        delete: Delete Project
      song:
        create: Create Song
        update: Update Song
        delete: Delete Song

  time:
    formats:
      default: "%a, %b %-d, %Y at %r"
      date: "%b %-d, %Y"
      short: "%B %d"

  titles:
    application: Edward Loveall
    posts:
      index: Edward Loveall
      show: "%{title} - Edward Loveall"
    admin:
      sessions:
        new: Sign in
      projects:
        index: Projects
        new: New project
        edit: Edit %{project}
      songs:
        index: Songs
        new: New song
        edit: Edit %{song}

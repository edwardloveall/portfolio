RSpec.describe 'Todo' do
  it 'fix the partial in views/admin/microposts/new.html.erb' do
    # I made some wacky routes initially for the microblog. I should either rethink the routes or also make standard rounds to go along with them so the partial rendere doesn't need a huge long path to render a micropost.
    expect(true).to eq(false)
  end
end
